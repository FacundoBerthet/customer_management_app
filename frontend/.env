# Variables de entorno para Vite (frontend)
# Nota: en Vite, SOLO las variables que empiecen con VITE_ se exponen al código del cliente.
# Este valor lo usamos en src/api/client.js como base de la API.
VITE_API_URL=http://localhost:8080/api/customers
